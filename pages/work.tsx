import React from "react";
import Layout from "../components/Layout";

type Props = {};

const Work: React.FC<Props> = (props: Props) => {
  return (
    <Layout>
      <div>work</div>
    </Layout>
  );
};

export default Work;
