import React from "react";
import Layout from "../components/Layout";

type Props = {};

const Blog: React.FC<Props> = (props: Props) => {
  return (
    <Layout>
      <div>blog</div>
    </Layout>
  );
};

export default Blog;
