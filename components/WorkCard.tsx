import React from "react";

type Props = {
  imgSrc: string;
  title: string;
  description: string;
  technologies: string[];
};

const WorkCard: React.FC<Props> = (props: Props) => {
  return <div></div>;
};

export default WorkCard;
